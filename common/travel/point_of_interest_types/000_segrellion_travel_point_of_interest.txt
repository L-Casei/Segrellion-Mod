# Segrellion custom Points of Interest.
# Loaded early so the Segrellion Lore POI is preferred when several POIs occupy the same province.

# =========================================================
# SEGRELLION HISTORICAL SITES
# =========================================================

segrellion_poi_dynastic_lore = {

	build_province_list = {

		# Deepdown, Skagos — c. 0 AC
		province:1595 = {
			add_to_list = provinces
		}

		# The Nightfort — c. 0 AC
		province:1 = {
			add_to_list = provinces
		}

		# Vhal — 106 AC onward
		province:4427 = {
			if = {
				limit = {
					current_date >= 8106.4.18
				}
				add_to_list = provinces
			}
		}

		# Bloodstone — 106–109 AC
		province:7294 = {
			if = {
				limit = {
					current_date >= 8106.1.1
				}
				add_to_list = provinces
			}
		}

		# Dyre Den — Dance of the Dragons
		province:4284 = {
			if = {
				limit = {
					current_date >= 8129.1.1
				}
				add_to_list = provinces
			}
		}

		# Tumbleton — 130 AC
		province:3967 = {
			if = {
				limit = {
					current_date >= 8130.1.1
				}
				add_to_list = provinces
			}
		}

		# The Beacon — c. 130 AC onward
		province:4373 = {
			if = {
				limit = {
					current_date >= 8130.1.1
				}
				add_to_list = provinces
			}
		}

		# White Hall — c. 150 AC onward
		province:4412 = {
			if = {
				limit = {
					current_date >= 8150.1.1
				}
				add_to_list = provinces
			}
		}

		# The Bloody Gate — late 2nd century AC
		province:2263 = {
			if = {
				limit = {
					current_date >= 8190.1.1
				}
				add_to_list = provinces
			}
		}

		# Castle Black — c. 195 AC
		province:5 = {
			if = {
				limit = {
					current_date >= 8195.1.1
				}
				add_to_list = provinces
			}
		}

		# The Shadow Tower — 2nd century AC
		province:8 = {
			if = {
				limit = {
					current_date >= 8195.1.1
				}
				add_to_list = provinces
			}
		}

		# Kingshouse, Skagos — after Aegon's disappearance
		province:1586 = {
			if = {
				limit = {
					current_date >= 8205.9.24
				}
				add_to_list = provinces
			}
		}

		# Gulltown — c. 275 AC onward
		province:2434 = {
			if = {
				limit = {
					current_date >= 8275.1.1
				}
				add_to_list = provinces
			}
		}

		# Duskendale — 277 AC
		province:4242 = {
			if = {
				limit = {
					current_date >= 8277.1.1
				}
				add_to_list = provinces
			}
		}

		# Lonely Light — c. 280s AC
		province:2854 = {
			if = {
				limit = {
					current_date >= 8280.1.1
				}
				add_to_list = provinces
			}
		}

		# The Trident / Ruby Ford — 283 AC
		province:1885 = {
			if = {
				limit = {
					current_date >= 8283.1.1
				}
				add_to_list = provinces
			}
		}
	}


	on_visit = {

		switch = {
			trigger = location

			province:4427 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_vhal }
						}
						trigger_event = segrellion_lore.0001
					}
				}
			}

			province:4373 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_beacon }
						}
						trigger_event = segrellion_lore.0002
					}
				}
			}

			province:4412 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_white_hall }
						}
						trigger_event = segrellion_lore.0003
					}
				}
			}

			province:3967 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_tumbleton }
						}
						trigger_event = segrellion_lore.0004
					}
				}
			}

			province:4284 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_dyre_den }
						}
						trigger_event = segrellion_lore.0005
					}
				}
			}

			province:7294 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_bloodstone }
						}
						trigger_event = segrellion_lore.0006
					}
				}
			}

			province:1885 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_trident }
						}
						trigger_event = segrellion_lore.0007
					}
				}
			}

			province:4242 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_duskendale }
						}
						trigger_event = segrellion_lore.0008
					}
				}
			}

			province:2434 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_gulltown }
						}
						trigger_event = segrellion_lore.0009
					}
				}
			}

			province:2263 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_bloody_gate }
						}
						trigger_event = segrellion_lore.0010
					}
				}
			}

			province:5 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_castle_black }
						}
						trigger_event = segrellion_lore.0011
					}
				}
			}

			province:1586 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_kingshouse }
						}
						trigger_event = segrellion_lore.0012
					}
				}
			}

			province:1595 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_deepdown }
						}
						trigger_event = segrellion_lore.0013
					}
				}
			}

			province:8 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_shadow_tower }
						}
						trigger_event = segrellion_lore.0014
					}
				}
			}

			province:1 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_nightfort }
						}
						trigger_event = segrellion_lore.0015
					}
				}
			}

			province:2854 = {
				root = {
					if = {
						limit = {
							NOT = { exists = var:segrellion_lore_visited_lonely_light }
						}
						trigger_event = segrellion_lore.0016
					}
				}
			}
		}

		custom_tooltip = segrellion_poi_dynastic_lore_tt
	}
}


segrellion_poi_firstflame_dragonlore = {
	build_province_list = {
		province:4427 = {
			if = {
				limit = {
					OR = {
						has_building = segrellion_fort_01
						has_building = segrellion_aeryns_den_02
						has_building = segrellion_firstflame_03
					}
				}
				add_to_list = provinces
			}
		}
	}

	on_visit = {
		root = { trigger_event = segrellion_dragonlore.0001 }
		custom_tooltip = segrellion_poi_firstflame_dragonlore_tt
	}
}