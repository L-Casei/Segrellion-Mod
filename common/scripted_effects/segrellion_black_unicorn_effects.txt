# =========================================================
# THE BLACK UNICORN
# SCRIPTED EFFECTS
# =========================================================


# =========================================================
# SELECT DAEMON'S INVESTIGATOR
#
# The investigator is permanently stored as a CHARACTER
# VARIABLE on Daemon.
# =========================================================

segrellion_black_unicorn_begin_courtier_investigation_effect = {


	if = {

		limit = {
			has_variable = segrellion_black_unicorn_investigator
		}

		remove_variable = segrellion_black_unicorn_investigator
	}


	random_courtier_or_guest = {

		limit = {

			is_alive = yes
			is_adult = yes

			intrigue >= 8

			NOT = {
				has_trait = incapable
			}

			NOT = {
				this = root
			}
		}


		alternative_limit = {

			is_alive = yes
			is_adult = yes

			NOT = {
				has_trait = incapable
			}

			NOT = {
				this = root
			}
		}


		save_scope_as = black_unicorn_investigator
	}


	if = {

		limit = {
			exists = scope:black_unicorn_investigator
		}


		set_variable = {

			name = segrellion_black_unicorn_investigator

			value = scope:black_unicorn_investigator
		}


		scope:black_unicorn_investigator = {

			add_character_flag = segrellion_black_unicorn_investigator
		}
	}


	add_character_flag = segrellion_black_unicorn_courtier_investigation


	trigger_event = {

		id = segrellion_black_unicorn.0020

		days = 90
	}
}

# =========================================================
# DUEL SAFETY
#
# The duel itself is REAL single combat.
# These effects only prevent its random injury system from
# permanently mutilating either story character.
# =========================================================

segrellion_black_unicorn_snapshot_duel_injuries_effect = {


	if = {
		limit = { has_trait = wounded_1 }
		add_character_flag = segrellion_bu_pre_duel_wounded_1
	}

	if = {
		limit = { has_trait = wounded_2 }
		add_character_flag = segrellion_bu_pre_duel_wounded_2
	}

	if = {
		limit = { has_trait = wounded_3 }
		add_character_flag = segrellion_bu_pre_duel_wounded_3
	}

	if = {
		limit = { has_trait = maimed }
		add_character_flag = segrellion_bu_pre_duel_maimed
	}

	if = {
		limit = { has_trait = one_legged }
		add_character_flag = segrellion_bu_pre_duel_one_legged
	}

	if = {
		limit = { has_trait = one_eyed }
		add_character_flag = segrellion_bu_pre_duel_one_eyed
	}

	if = {
		limit = { has_trait = disfigured }
		add_character_flag = segrellion_bu_pre_duel_disfigured
	}

	if = {
		limit = { has_trait = scarred }
		add_character_flag = segrellion_bu_pre_duel_scarred
	}
}


segrellion_black_unicorn_restore_duel_injuries_effect = {


	if = {
		limit = { has_trait = wounded_1 }
		remove_trait = wounded_1
	}

	if = {
		limit = { has_trait = wounded_2 }
		remove_trait = wounded_2
	}

	if = {
		limit = { has_trait = wounded_3 }
		remove_trait = wounded_3
	}

	if = {
		limit = { has_trait = maimed }
		remove_trait = maimed
	}

	if = {
		limit = { has_trait = one_legged }
		remove_trait = one_legged
	}

	if = {
		limit = { has_trait = one_eyed }
		remove_trait = one_eyed
	}

	if = {
		limit = { has_trait = disfigured }
		remove_trait = disfigured
	}

	if = {
		limit = { has_trait = scarred }
		remove_trait = scarred
	}


	if = {
		limit = { has_character_flag = segrellion_bu_pre_duel_wounded_1 }
		add_trait = wounded_1
	}

	if = {
		limit = { has_character_flag = segrellion_bu_pre_duel_wounded_2 }
		add_trait = wounded_2
	}

	if = {
		limit = { has_character_flag = segrellion_bu_pre_duel_wounded_3 }
		add_trait = wounded_3
	}

	if = {
		limit = { has_character_flag = segrellion_bu_pre_duel_maimed }
		add_trait = maimed
	}

	if = {
		limit = { has_character_flag = segrellion_bu_pre_duel_one_legged }
		add_trait = one_legged
	}

	if = {
		limit = { has_character_flag = segrellion_bu_pre_duel_one_eyed }
		add_trait = one_eyed
	}

	if = {
		limit = { has_character_flag = segrellion_bu_pre_duel_disfigured }
		add_trait = disfigured
	}

	if = {
		limit = { has_character_flag = segrellion_bu_pre_duel_scarred }
		add_trait = scarred
	}


	remove_character_flag = segrellion_bu_pre_duel_wounded_1
	remove_character_flag = segrellion_bu_pre_duel_wounded_2
	remove_character_flag = segrellion_bu_pre_duel_wounded_3

	remove_character_flag = segrellion_bu_pre_duel_maimed
	remove_character_flag = segrellion_bu_pre_duel_one_legged
	remove_character_flag = segrellion_bu_pre_duel_one_eyed
	remove_character_flag = segrellion_bu_pre_duel_disfigured
	remove_character_flag = segrellion_bu_pre_duel_scarred
}

# =========================================================
# REVEAL THE BLACK UNICORN
#
# DO NOT CALL THIS EFFECT YET.
#
# The same character becomes Visenya Segrellion.
# Her genealogy only appears after the reveal.
# =========================================================

segrellion_reveal_black_unicorn_identity_effect = {


	character:Black_Unicorn_1 = {


		change_first_name = segrellion_black_unicorn_true_name


		set_father = character:Segrellion_55
		set_mother = character:Segrellion_59


		set_house = house:house_Segrellion


		remove_character_flag = segrellion_black_unicorn_mysterious_woman

		add_character_flag = segrellion_black_unicorn_identity_revealed
		add_character_flag = segrellion_black_unicorn_visenya_revealed
	}
}

# =========================================================
# RETURN TO DAEMON'S ORIGINAL CAMP
#
# Uses the province stored before the Skagos expedition.
# =========================================================

segrellion_black_unicorn_return_to_daemon_camp_effect = {


	# Save wherever Daemon physically is when the return begins.

	location = {

		save_scope_as = black_unicorn_return_start
	}


	if = {

		limit = {

			exists = scope:black_unicorn_return_start
		}


		set_variable = {

			name = segrellion_black_unicorn_return_start

			value = scope:black_unicorn_return_start
		}
	}


	if = {

		limit = {

			exists = current_travel_plan
		}


		current_travel_plan = {

			complete_travel_plan = yes
		}
	}


	trigger_event = {

		id = segrellion_black_unicorn.0195

		days = 1
	}
}


# =========================================================
# VISENYA - RESOLVE SECRET OF SKAGOS
#
# ROOT = Black_Unicorn_1
#
# The result is stored directly on She-Unicorn.
# This effect is idempotent.
# =========================================================

segrellion_black_unicorn_resolve_secret_effect = {

	if = {

		limit = {

			NOT = {
				has_character_flag = segrellion_black_unicorn_secret_icebringer
			}

			NOT = {
				has_character_flag = segrellion_black_unicorn_secret_rhaegon
			}
		}


		# =================================================
		# ICEBRINGER
		# =================================================

		if = {

			limit = {
				has_game_rule = segrellion_visenya_secret_icebringer
			}


			add_character_flag = segrellion_black_unicorn_secret_icebringer
		}


		# =================================================
		# RHAEGON
		# =================================================

		else_if = {

			limit = {
				has_game_rule = segrellion_visenya_secret_rhaegon
			}


			add_character_flag = segrellion_black_unicorn_secret_rhaegon
		}


		# =================================================
		# RANDOM
		# =================================================

		else_if = {

			limit = {
				has_game_rule = segrellion_visenya_secret_random
			}


			random_list = {

				50 = {
					add_character_flag = segrellion_black_unicorn_secret_icebringer
				}

				50 = {
					add_character_flag = segrellion_black_unicorn_secret_rhaegon
				}
			}
		}


		# =================================================
		# DEFENSIVE FALLBACK
		#
		# Preserve the current mod behavior.
		# =================================================

		else = {

			add_character_flag = segrellion_black_unicorn_secret_icebringer
		}
	}
}


# =========================================================
# CREATE SECRET RHAEGON
#
# ROOT = Black_Unicorn_1
#
# Rhaegon already exists when Daemon begins investigating.
#
# This deliberately does NOT use
# agot_spawn_owned_hatchling_from_egg_effect because that
# effect sends the owner a hatching interface message.
#
# There is no physical egg here because its discovery and
# hatching happened before the playable storyline.
# =========================================================

segrellion_black_unicorn_create_rhaegon_effect = {

	if = {

		limit = {

			NOT = {
				exists = global_var:segrellion_historical_rhaegon
			}
		}


		save_scope_as = rhaegon_owner


		# =================================================
		# CREATE RHAEGON
		#
		# Direct creation.
		# NO hatch effect.
		# NO interface notification.
		# =================================================

		create_character = {

			name = Rhaegon

			location = root.location

			template = agot_dragon_hatchling_template

			age = 0

			random_traits = no

			save_scope_as = rhaegon
		}


		# =================================================
		# OWNERSHIP
		# =================================================

		hidden_effect = {

			agot_set_as_owned_dragon = {

				OWNER = scope:rhaegon_owner
				DRAGON = scope:rhaegon
			}
		}


		# =================================================
		# RHAEGON TRAITS
		# =================================================

		scope:rhaegon = {

			change_first_name = Rhaegon


			add_character_flag = segrellion_rhaegon_dragon
			add_character_flag = segrellion_rhaegon_secret
			add_character_flag = agot_no_silly_name


			# =============================================
			# PHYSIQUE
			# =============================================

			remove_trait = dragon_physique_bad
			remove_trait = dragon_physique_good_1
			remove_trait = dragon_physique_good_2
			remove_trait = dragon_physique_good_3

			# Tyrannic Leviathan
			add_trait = dragon_physique_good_3


			# =============================================
			# SPECIAL
			# =============================================

			add_trait = dragon_majestic


			# =============================================
			# PERSONALITY
			# =============================================

			add_trait = dragon_bloodthirsty
			add_trait = dragon_voracious
			add_trait = dragon_calculating
			add_trait = dragon_imperious


			# =============================================
			# GROWTH-APPROPRIATE PROWESS
			#
			# Our historical dragon growth system gives
			# +3 prowess alongside +3 size during early
			# growth.
			#
			# Size 30 therefore corresponds to 30 prowess.
			# =============================================

			add_prowess_skill = 30


			# =============================================
			# RHAEGON DNA
			#
			# Still intentionally absent.
			#
			# DO NOT add has_scripted_appearance until
			# Rhaegon's actual dragon DNA is supplied.
			# =============================================
		}


		# =================================================
		# AGOT DRAGON RUNTIME STORAGE
		#
		# This is the part currently missing.
		# =================================================

		segrellion_initialize_runtime_dragon_stats_effect = {

			DRAGON = scope:rhaegon
			DRAGON_AGE = scope:rhaegon.age
			DRAGON_SIZE = 30
		}


		# Bloodthirsty receives +4 Draconic Dread when AGOT
		# normally assigns the personality through its
		# personality event. We reproduce that initialization
		# because the trait is being assigned directly.

		scope:rhaegon = {

			change_draconic_dread = {
				VALUE = 4
			}
		}


		# =================================================
		# AGOT DRAGON SYSTEM
		# =================================================

		agot_dragon_tree_creation_effect = {

			DRAGON = scope:rhaegon
		}


		agot_bond_dragon_relation_effect = {

			ACTOR = scope:rhaegon_owner
			DRAGON = scope:rhaegon
		}


		# =================================================
		# PERMANENT REFERENCE
		# =================================================

		set_global_variable = {

			name = segrellion_historical_rhaegon

			value = scope:rhaegon
		}


		add_character_flag = segrellion_black_unicorn_rhaegon_created
	}
}