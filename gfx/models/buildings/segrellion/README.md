# Model placeholder

Put the final exported files here:

- `segrellion_beacon.mesh`
- `segrellion_beacon.asset`
- `segrellion_beacon_diffuse.dds`
- `segrellion_beacon_normal.dds`
- `segrellion_beacon_properties.dds`

The building file needs an `asset = { ... }` block that matches the mesh or entity
name declared in the `.asset` file.
